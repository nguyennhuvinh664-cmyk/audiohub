services:
  - type: web
    name: audiohub
    runtime: node
    buildCommand: cd backend && npm install && npm run build
    startCommand: cd backend && npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DISABLE_PRISMA_MIGRATE
        value: "true"
