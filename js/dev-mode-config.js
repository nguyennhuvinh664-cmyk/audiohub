/**
 * AudioHub Developer Mode Config
 * ───────────────────────────────
 * Set DEV_MODE = true  → Developer Mode ON
 * Set DEV_MODE = false → Developer Mode OFF (no DOM injected, no code runs)
 *
 * For production: set to false or remove this file entirely.
 */
window.DEV_MODE = false;
